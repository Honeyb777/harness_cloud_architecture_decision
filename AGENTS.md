# Repository 작업 지침

이 저장소는 Cloud Architecture Decision Harness다. 문서와 의사결정 증적이 주요 산출물이며, 현재 실행 애플리케이션이나 배포 환경은 없다.

## 시작 순서

1. `README.md`, `PROMPT.md`, `sot/` 전체를 읽는다.
2. 작업에 필요한 `agents/` 역할 및 `templates/`를 읽는다.
3. 사용자가 지정한 Assessment를 우선한다. 지정하지 않았다면 `assessments/README.md`를 확인한다. 다른 서비스·환경의 검토를 임의로 이어 쓰거나 대상 미정 검토를 생성하지 않는다.
4. 현재 Step, Phase, 선행 Gate, 미결정 항목을 확인한다.

## 작업 원칙

- 사용자 지시를 우선하며 저장소 정책은 그 범위 안에서 적용한다.
- 문서 정리, 템플릿 보완, 로컬 기본 설정은 `AUTO`로 진행한다.
- 주요 아키텍처 선택은 `sot/DECISION-POLICY.md`를 따른다. 이미 받은 결정은 증거를 연결하고 다시 묻지 않는다.
- `agents/`는 전문 검토 역할 정의다. 별도 에이전트 실행 설정으로 간주하지 않는다.
- 확인된 사실, 가정, 미확인 정보를 구분한다. 제공되지 않은 요구사항과 사용자 결정을 만들지 않는다.
- Recommendation, User Decision, Implemented Reality를 각각 기록한다. 설계 채택을 실제 적용으로 기록하지 않는다.
- 상태 및 Gate 해석은 `sot/WORKFLOW.md`를 따른다. 선행 Gate가 미충족이면 후속 조사와 대안 초안은 가능하지만 설계 확정이나 적용으로 넘어가지 않는다.
- 변동 가능한 가격·기능·리전 지원은 검토 시점의 공식 출처와 확인일을 기록한다.
- 자격증명, 개인정보, 원본 민감 로그를 저장하지 않는다. 증적에는 필요한 요약과 접근 가능한 참조만 남긴다.

## 완료 확인

- 변경한 문서의 상대 링크와 참조 경로를 확인한다.
- Assessment와 Decision register의 상태 및 링크를 일치시킨다.
- 완료하지 않은 검토를 `ACCEPTED`로 표시하지 않는다.
- 주요 변경은 `history/`에 요약한다. 설정 작업과 아키텍처 결정을 구분한다.
- 커밋·푸시·실제 클라우드 적용 여부는 수행한 사실만 보고한다.

## 반복 검토 및 역할 운영

- 검토별 구조와 환경 경계는 `sot/ASSESSMENT-STRUCTURE.md`를 따른다. 각 검토의 결정·보고서·평가·이력은 해당 Assessment 안에 둔다.
- `agents/README.md`에서 필요한 역할을 선택하고 매 사용 후 `sot/AGENT-LIFECYCLE.md`에 따라 평가한다. 평가 근거에 따라 역할을 추가·보완·통합하거나 비활성화한다.
- DevOps/CI/CD와 애플리케이션 빌드·런타임, 네트워크/기존 IDC 연동의 적용 여부를 모든 검토의 Discovery에서 확인한다.
- 커밋 메시지를 요청받으면 설명·접두 문구·본문 없이 변경 내용을 요약한 한 줄 한국어 메시지를 작성한다. 메시지 작성 요청은 커밋 실행 요청으로 간주하지 않는다.
